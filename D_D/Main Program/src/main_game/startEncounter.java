package main_game;

import javax.swing.JPanel;

public class startEncounter extends encounter
{
	
	public startEncounter(scrollButtons [] button, textLabels[] label, JPanel cloumnpanel, JPanel cloumntextpanel)
	{
		Main.columntextpanel.removeAll();
		Main.columnpanel.removeAll();	
	}
}
